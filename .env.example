# Camera Settings
CAMERA_INDEX=0
MIN_AREA_THRESHOLD=5000  # Threshold for motion detection (pixels)
MOTION_WAIT_TIME=1.5     # Seconds of stillness required for capture
ECO_MODE_DELAY=10.0      # Seconds of no motion before entering Eco mode

# OCR Settings (Tesseract - Only required if not using EasyOCR)
# TESSERACT_PATH=C:\Program Files\Tesseract-OCR\tesseract.exe

# System Settings
TEMP_DIR=./temp
RETENTION_DAYS=1
